# Sensitive business / legal documents — contains EIN, formation docs, etc.
memory/
legal/

# API keys and secrets — never commit
Secrets.plist
**/Secrets.plist
*.secrets.plist

# Apple private keys — APNs auth keys, StoreKit/subscription keys (.p8)
*.p8
**/*.p8

# Python
__pycache__/

# macOS
.DS_Store

.claude/settings.local.json
**/.claude/settings.local.json
